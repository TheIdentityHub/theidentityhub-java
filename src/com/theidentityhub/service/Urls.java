package com.theidentityhub.service;

/**
 * The Class Urls.
 * 
 * @author U2U Consult
 */
class Urls {

     /** The Constant AUTH_ENDPOINT. */
     public final static String AUTH_ENDPOINT = "/oauth2/v1/auth/";

     /** The Constant VERIFY_ENDPOINT. */
     public final static String VERIFY_ENDPOINT = "/oauth2/v1/verify/";

     /** The Constant REVOKE_ENDPOINT. */
     public final static String REVOKE_ENDPOINT = "/oauth2/v1/revoke/";

     /** The Constant PROVIDER_ID. */
     public final static String PROVIDER_ID = "&providerId=";

     /** The Constant API_ENDPOINT. */
     public final static String API_ENDPOINT = "/api/identity/v1";

     /** The Constant ACCOUNTS. */
     public final static String ACCOUNTS = "/accounts";

     /** The Constant ROLES. */
     public final static String ROLES = "/roles";

     /** The Constant FRIENDS. */
     public final static String FRIENDS = "/friends";

     /** The offset param. */
     public static String OFFSET_PARAM;

     /** The limit param. */
     public static String LIMIT_PARAM;

     /** The expected auth response. */
     public static String EXPECTED_AUTH_RESPONSE;

     /** The access token param. */
     public static String ACCESS_TOKEN_PARAM;

     /** The auth url. */
     public static String AUTH_URL;

     /** The verify uri. */
     public static String VERIFY_URI;

     /** The revoke uri. */
     public static String REVOKE_URI;

     /** The identity uri. */
     public static String IDENTITY_URI;

     /** The identity uri with id. */
     public static String IDENTITY_URI_WITH_ID;

     /** The accounts uri. */
     public static String ACCOUNTS_URI;

     /** The accounts uri with id. */
     public static String ACCOUNTS_URI_WITH_ID;

     /** The friends uri. */
     public static String FRIENDS_URI;

     /** The friends uri with id. */
     public static String FRIENDS_URI_WITH_ID;

     /** The roles uri. */
     public static String ROLES_URI;

     /** The roles uri with id. */
     public static String ROLES_URI_WITH_ID;

}
